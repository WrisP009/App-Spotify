import { NoimagePipePipe } from './noimage-pipe.pipe';

describe('NoimagePipePipe', () => {
  it('create an instance', () => {
    const pipe = new NoimagePipePipe();
    expect(pipe).toBeTruthy();
  });
});
